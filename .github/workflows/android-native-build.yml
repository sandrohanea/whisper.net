name: Android Native build
permissions:
  contents: read

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      BuildConfig:
        required: true
        type: string
        default: "Release"

jobs:
  native-build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true

      - name: Install Android NDK
        uses: nttld/setup-ndk@aacaf74664d0f8e1b070a865acb47ee25007386d
        with:
          ndk-version: r25c

      - name: Run android build
        run: make android BUILD_TYPE=${{ inputs.BuildConfig }}

      - name: Upload Android Build Artifacts
        uses: actions/upload-artifact@v6
        with:
          name: android-build
          path: "runtimes/Whisper.net.Run*/android-*/*"
          retention-days: 7
